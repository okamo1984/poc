syntax = "proto3";

package buf.connect.demo.count.v1;

message CountRequest {
    int64 add = 1;
}

message CountResponse {
    int64 count = 1;
}

service BigIntService {
    rpc Count(CountRequest) returns (CountResponse);
}